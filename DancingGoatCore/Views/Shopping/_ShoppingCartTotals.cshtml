@model DancingGoat.Models.CartViewModel

@{
    var tax = Model.TotalTax;
    var shipping = Model.TotalShipping;
}

@if (tax > 0)
{
    <div class="cart-total-viewer">
        <div class="label">
            @HtmlLocalizer["Total tax"]
        </div>
        <div class="value">
            @Model.FormatPrice(tax)
        </div>
    </div>
}

@if (shipping > 0)
{
    <div class="cart-total-viewer">
        <div class="label">
            @HtmlLocalizer["Shipping"]
        </div>
        <div class="value">
            @Model.FormatPrice(shipping)
        </div>
    </div>
}


@if ((tax > 0) || (shipping > 0))
{
    <hr />
}

<div class="cart-total">
    <div class="cart-total-viewer">
        <div class="label">
            <span>@HtmlLocalizer["Total Payment: "]</span>
        </div>
        <div class="value">
            <h3 style="color:darkred">@Model.FormatPrice(Model.GrandTotal)</h3>
        </div>
    </div>
</div>
